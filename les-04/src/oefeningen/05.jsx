/***
 Oefening 5:

 1. Maak de volgende HTML na in JSX:

<p>
  Mijn naam is "NAAM", ik ben al "AANTAL JAAR" jaar developer.
</p>

 2. Gebruik de `name` en `since` properties van het component.
 3. Aantal jaar reken je uit door `jaartal nu - since` te doen. bijv 2023 - 2016 = 7
 4. Importeer en render dit component in `./src/App.jsx` en geef `name` en `since` mee.
***/

function Oefening05({ name = 'Gaya', since = 2016 }) {
  return (
    null
  );
}

export default Oefening05;
